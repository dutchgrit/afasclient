<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- Beta release: OAuth support (Client Credentials + Bearer token) has not shipped from main yet.
         Re-pin to the stable 2.3.0.x release once it is published. -->
    <PackageReference Include="DutchGrit.AfasClient" Version="2.3.0.38-beta" />
  </ItemGroup>

</Project>
